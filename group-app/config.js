// Fill these two lines in with your own Supabase project's details, then
// save the file. Both values come from: supabase.com dashboard -> your
// project -> the gear icon ("Project Settings") -> "API" in the sidebar.
//
// - SUPABASE_URL is the "Project URL" near the top of that page.
// - SUPABASE_ANON_KEY is the "anon" "public" key further down the page.
//
// It's fine for these to be visible in the app's code, even once this is
// on a public website — the "anon" key is specifically designed to be
// public, and the schema.sql file's lock-down rules are what actually
// keep people's data private, not secrecy of this key.
//
// Never put the "service_role" key (also on that page) here or anywhere
// in this app — that one bypasses every lock-down rule and must stay
// private to the Supabase dashboard only.

const SUPABASE_URL = "https://ggbmuzrzrbktbljiyvtf.supabase.co";
const SUPABASE_ANON_KEY = "sb_publishable_yauYiVbZviZb5QN0xxEQLw_U7f6ymmg"
